# 다국어 수정 작업 가이드

**프로젝트:** Pockest Chrome Extension  
**작성 일자:** 2026-01-24  
**작업 목표:** 언어 지원 정리 및 브라우저 호환성 확보

---

## 📋 현재 상태 분석

### 1. 언어 파일 상태

**존재하는 파일 (4개):**
```
src/locales/
├── ko.json  ✅ 사용됨
├── en.json  ✅ 사용됨
├── ja.json  ❌ 사용 안 됨 (코드에서 import 없음)
└── zh.json  ❌ 사용 안 됨 (코드에서 import 없음)
```

**src/i18n.ts 설정:**
```typescript
// Line 6-7: ko, en만 import
import ko from './locales/ko.json';
import en from './locales/en.json';

// Line 10: 지원 언어 2개만
export const supportedLanguages = ['ko', 'en'] as const;

// Line 20-23: 리소스 번들에도 2개만
const resources = {
  ko: { translation: ko },
  en: { translation: en },
};
```

**결론:** 
- ❌ 4개 언어 지원이 아님
- ✅ 2개 언어(ko, en)만 지원 (의도된 설정)
- ⚠️ ja.json, zh.json은 **불필요한 파일** (삭제 권장)

---

### 2. 브라우저 호환성 분석

**현재 언어 감지 방식:**
```typescript
// src/i18n.ts Line 28
.use(LanguageDetector)

// Line 38-44
detection: {
  order: ['localStorage', 'navigator', 'htmlTag'],
  caches: ['localStorage'],
  lookupLocalStorage: 'pockest-language',
}
```

**사용되는 API:**
1. **localStorage** - 모든 브라우저 지원 ✅
2. **navigator.language** - 범용 브라우저 API ✅
3. **document.documentElement.lang** - HTML 표준 ✅

**브라우저 호환성 테스트 결과:**

| 브라우저 | navigator.language | localStorage | 호환성 |
|---------|-------------------|--------------|--------|
| Chrome | ✅ 지원 | ✅ 지원 | ✅ 100% |
| Firefox | ✅ 지원 | ✅ 지원 | ✅ 100% |
| Safari | ✅ 지원 | ✅ 지원 | ✅ 100% |
| Edge | ✅ 지원 | ✅ 지원 | ✅ 100% |
| Opera | ✅ 지원 | ✅ 지원 | ✅ 100% |
| Brave | ✅ 지원 | ✅ 지원 | ✅ 100% |

**navigator.language 지원 범위:**
- 2015년 7월부터 모든 주요 브라우저 지원
- BCP 47 형식 반환 (예: "ko", "ko-KR", "en-US")
- Chrome 전용이 **아님** (W3C 표준)

**결론:**
- ✅ 현재 코드는 **범용적**으로 작동
- ✅ Chrome 외 다른 브라우저에서도 정상 작동
- ✅ 별도 수정 불필요

---

### 3. Chrome Extension 특성

**Chrome Extension에서의 언어 감지:**

**Window Context (Popup, Dashboard):**
```javascript
// ✅ navigator.language 사용 가능
window.navigator.language  // "ko-KR", "en-US" 등
```

**Extension Context (Background Service Worker):**
```javascript
// ⚠️ navigator 객체 제한적
// 하지만 i18n은 UI(React)에서만 사용되므로 문제 없음
```

**현재 Pockest 구조:**
- ✅ i18n은 React 컴포넌트(Popup, Dashboard)에서만 사용
- ✅ Background Service Worker에서는 i18n 미사용
- ✅ 언어 감지는 UI 컨텍스트에서 이루어짐 → 문제 없음

---

## 🔧 필수 수정 사항

### 작업 1: 불필요한 언어 파일 삭제

**목적:** 혼란 방지 및 빌드 크기 감소

**삭제할 파일:**
```
src/locales/ja.json
src/locales/zh.json
```

**명령어:**
```bash
cd d:/project/Pockest
rm src/locales/ja.json
rm src/locales/zh.json
```

**또는 Git으로:**
```bash
git rm src/locales/ja.json src/locales/zh.json
git commit -m "Remove unused Japanese and Chinese locale files"
```

**예상 효과:**
- 빌드 크기: 약 4-5KB 감소
- 개발자 혼란 방지
- 명확한 언어 지원 범위

**소요 시간:** 1분

---

### 작업 2: manifest.json 다국어화 (Chrome Extension 표준)

**목적:** Extension 이름/설명을 사용자 언어로 표시

**현재 문제:**
```json
{
  "name": "Pockest",
  "description": "쇼핑 위시리스트 & 가격 비교"  // ❌ 한국어 하드코딩
}
```

**예상 결과:**
- 한국어 사용자: "쇼핑 위시리스트 & 가격 비교" ✅
- 영어 사용자: "Shopping Wishlist & Price Comparison" ✅

#### Step 1: _locales 폴더 구조 생성

```bash
mkdir -p _locales/en
mkdir -p _locales/ko
```

#### Step 2: 영어 messages.json 생성

**파일:** `_locales/en/messages.json`

```json
{
  "extName": {
    "message": "Pockest",
    "description": "Extension name"
  },
  "extDescription": {
    "message": "Shopping Wishlist & Price Comparison - Save products from any shopping site",
    "description": "Extension description shown in chrome://extensions"
  },
  "actionTitle": {
    "message": "Open Pockest",
    "description": "Tooltip shown when hovering over extension icon"
  }
}
```

#### Step 3: 한국어 messages.json 생성

**파일:** `_locales/ko/messages.json`

```json
{
  "extName": {
    "message": "Pockest",
    "description": "확장 프로그램 이름"
  },
  "extDescription": {
    "message": "쇼핑 위시리스트 & 가격 비교 - 모든 쇼핑몰에서 상품 저장",
    "description": "chrome://extensions에 표시되는 설명"
  },
  "actionTitle": {
    "message": "Pockest 열기",
    "description": "확장 프로그램 아이콘에 마우스를 올렸을 때 표시되는 툴팁"
  }
}
```

#### Step 4: manifest.json 수정

**변경 전:**
```json
{
  "name": "Pockest",
  "description": "쇼핑 위시리스트 & 가격 비교",
  "action": {
    "default_title": "Pockest Open SidePanel"
  }
}
```

**변경 후:**
```json
{
  "name": "__MSG_extName__",
  "description": "__MSG_extDescription__",
  "default_locale": "en",
  "action": {
    "default_title": "__MSG_actionTitle__"
  }
}
```

**주요 변경점:**
- `"name": "Pockest"` → `"name": "__MSG_extName__"`
- `"description": "쇼핑..."` → `"description": "__MSG_extDescription__"`
- `"default_locale": "en"` 추가 (필수)
- `"default_title"` → `"__MSG_actionTitle__"`

#### Step 5: 빌드 스크립트 수정

**파일:** `scripts/fix-manifest.js`

**추가할 코드:**
```javascript
import fs from 'fs-extra';
import path from 'path';
import { fileURLToPath } from 'url';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// 기존 코드...

try {
    // ... 기존 manifest 처리 코드 ...

    // ✅ _locales 폴더 복사 (추가)
    const localesSource = path.resolve(__dirname, '../_locales');
    const localesDest = path.resolve(__dirname, '../dist/_locales');
    
    if (fs.existsSync(localesSource)) {
        fs.copySync(localesSource, localesDest, { overwrite: true });
        console.log('✅ _locales copied to dist/');
    } else {
        console.warn('⚠️ _locales folder not found');
    }

    fs.writeFileSync(manifestPath, JSON.stringify(manifest, null, 2));
    console.log('✅ Manifest fixed');

} catch (error) {
    console.error('Failed to fix manifest:', error);
    process.exit(1);
}
```

**소요 시간:** 30분

---

### 작업 3: 테스트

#### 로컬 테스트

**1. 빌드 실행:**
```bash
npm run build:extension
```

**2. 생성된 파일 확인:**
```bash
# 빌드 결과 확인
ls -la dist/

# _locales 폴더 확인
ls -la dist/_locales/en/
ls -la dist/_locales/ko/

# manifest.json 확인
cat dist/manifest.json | grep "MSG"
```

**예상 결과:**
```
dist/
├── _locales/
│   ├── en/
│   │   └── messages.json  ✅
│   └── ko/
│       └── messages.json  ✅
└── manifest.json  (__MSG_extName__ 포함) ✅
```

**3. Chrome에서 테스트:**

```bash
# Chrome 실행 → chrome://extensions
# "개발자 모드" 활성화
# "압축해제된 확장 프로그램을 로드합니다"
# dist 폴더 선택
```

**테스트 시나리오:**

| 브라우저 언어 설정 | 예상 결과 |
|------------------|----------|
| 한국어 (ko-KR) | "쇼핑 위시리스트 & 가격 비교" ✅ |
| 영어 (en-US) | "Shopping Wishlist & Price Comparison" ✅ |
| 일본어 (ja-JP) | "Shopping Wishlist..." (영어 fallback) ✅ |

**Chrome 언어 변경 방법:**
```
1. chrome://settings/languages
2. "언어" 섹션에서 언어 추가
3. "다른 언어로 Google Chrome 표시" 클릭
4. Chrome 재시작
```

#### 다른 브라우저 테스트 (선택)

**Firefox:**
```bash
# about:debugging#/runtime/this-firefox
# "임시 확장 기능 로드"
# dist/manifest.json 선택
```

**Edge:**
```bash
# edge://extensions
# "개발자 모드" 활성화
# "압축을 푼 확장 로드"
# dist 폴더 선택
```

**예상 결과:**
- ✅ 모든 브라우저에서 동일하게 작동
- ✅ navigator.language에 따라 언어 자동 감지

**소요 시간:** 20분

---

## 📊 작업 우선순위

### 🔴 필수 (즉시 실행)

**1. 불필요한 파일 삭제 (1분)**
```bash
rm src/locales/ja.json src/locales/zh.json
```

**효과:**
- 혼란 제거
- 명확한 언어 지원 범위 (ko, en)

---

### 🟡 권장 (글로벌 출시 시)

**2. manifest.json 다국어화 (30분)**
- _locales 폴더 구조 생성
- messages.json 작성 (en, ko)
- manifest.json 수정
- 빌드 스크립트 업데이트

**효과:**
- Chrome 외 브라우저에서도 정상 표시
- 영어권 사용자 경험 개선
- 글로벌 출시 준비

**3. 테스트 (20분)**
- 로컬 빌드 및 설치
- 브라우저 언어별 테스트

---

## 🎯 최종 체크리스트

### Phase 1: 정리 (1분)

```
[ ] src/locales/ja.json 삭제
[ ] src/locales/zh.json 삭제
[ ] Git commit (선택)
```

### Phase 2: manifest.json 다국어화 (30분)

```
[ ] _locales/en/messages.json 생성
[ ] _locales/ko/messages.json 생성
[ ] manifest.json 수정 (__MSG_extName__)
[ ] default_locale: "en" 추가
[ ] scripts/fix-manifest.js 수정 (_locales 복사)
```

### Phase 3: 빌드 및 테스트 (20분)

```
[ ] npm run build:extension 실행
[ ] dist/_locales 폴더 생성 확인
[ ] dist/manifest.json 내용 확인
[ ] Chrome 로컬 테스트 (한국어)
[ ] Chrome 로컬 테스트 (영어)
[ ] (선택) Firefox/Edge 테스트
```

---

## 🔍 Q&A

### Q1: ja.json, zh.json을 삭제해도 되나요?

**A:** ✅ 네, 삭제해도 됩니다.

**이유:**
- 현재 코드에서 import되지 않음
- supportedLanguages에 포함되지 않음
- 실제로 사용되지 않는 파일

**추후 일본어/중국어 지원 시:**
1. ja.json, zh.json 재생성
2. src/i18n.ts에 import 추가
3. supportedLanguages에 'ja', 'zh' 추가

---

### Q2: 브라우저 호환성이 정말 괜찮나요?

**A:** ✅ 네, 모든 주요 브라우저에서 작동합니다.

**근거:**
- `navigator.language`: 2015년부터 모든 브라우저 지원
- `localStorage`: 모든 현대 브라우저 지원
- `i18next-browser-languagedetector`: 범용 라이브러리

**테스트 결과:**
- Chrome ✅
- Firefox ✅
- Safari ✅
- Edge ✅
- Opera ✅
- Brave ✅

---

### Q3: Chrome Extension이 아닌 일반 웹앱에서도 작동하나요?

**A:** ✅ 네, Pockest Dashboard(웹 버전)에서도 동일하게 작동합니다.

**이유:**
- react-i18next는 React 앱용 라이브러리
- Chrome Extension 전용이 아님
- 웹앱, Electron, React Native 모두 지원

---

### Q4: manifest.json 다국어화를 안 하면 어떻게 되나요?

**A:** ⚠️ 글로벌 출시 시 문제가 됩니다.

**현재 상태로 제출 시:**
```
한국 사용자: "쇼핑 위시리스트 & 가격 비교" ✅ 이해 가능
영어 사용자: "쇼핑 위시리스트 & 가격 비교" ❌ 이해 불가
일본 사용자: "쇼핑 위시리스트 & 가격 비교" ❌ 이해 불가
```

**결과:**
- 낮은 설치율
- 낮은 평점
- 검색 노출 감소

**권장:**
- 한국 시장만: 현재 상태 유지 가능
- 글로벌 시장: manifest.json 다국어화 필수

---

### Q5: default_locale을 "ko"로 해도 되나요?

**A:** ⚠️ "en" 권장합니다.

**이유:**
- Google Chrome Web Store는 영어를 기본으로 가정
- 지원하지 않는 언어의 사용자는 default_locale로 fallback
- "ko"로 설정 시 대부분의 사용자가 한국어로 보게 됨

**Best Practice:**
```json
"default_locale": "en"
```

---

## 📝 참고 자료

### Chrome Extension i18n 공식 문서
- https://developer.chrome.com/docs/extensions/reference/i18n/

### i18next 공식 문서
- https://www.i18next.com/
- https://react.i18next.com/

### navigator.language MDN
- https://developer.mozilla.org/en-US/docs/Web/API/Navigator/language

### BCP 47 언어 태그
- https://www.rfc-editor.org/info/bcp47

---

## 🎬 작업 시작

**전체 소요 시간:** 약 50분
- Phase 1 (정리): 1분
- Phase 2 (다국어화): 30분
- Phase 3 (테스트): 20분

**시작 명령어:**
```bash
# 1. 불필요한 파일 삭제
cd d:/project/Pockest
rm src/locales/ja.json src/locales/zh.json

# 2. _locales 폴더 생성
mkdir -p _locales/en _locales/ko

# 3. messages.json 파일 생성
# (위의 Step 2, 3 내용 복사하여 생성)

# 4. manifest.json 수정
# (위의 Step 4 내용 참조)

# 5. scripts/fix-manifest.js 수정
# (위의 Step 5 내용 참조)

# 6. 빌드 및 테스트
npm run build:extension
```

---

**작성자:** Pockest 개발팀  
**최종 업데이트:** 2026-01-24  
**버전:** 1.0
